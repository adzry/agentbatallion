/**
 * Temporal client placeholder.
 * Phase 1 uses direct in-process mock generation (see src/web/server.ts).
 */
export function createTemporalClient() {
  throw new Error("Temporal client not implemented (Phase 1)");
}
